# 统一换行符：仓库里一律存 LF，工作区也检出 LF。
#
# 为什么需要：Windows 上 Git 默认 core.autocrlf=true，会在检出时把文件改成 CRLF，
# 于是每次 add 都刷一屏 "LF will be replaced by CRLF" 警告，且跨平台协作时
# 会因为换行符产生大量假 diff。本项目是 Node/TypeScript，LF 完全可用。
* text=auto eol=lf

# 二进制文件不要做任何换行符转换
*.png binary
*.jpg binary
*.webp binary
*.gif binary
*.ico binary
