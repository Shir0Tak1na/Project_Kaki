{
  "name": "fictional-cartographer",
  "version": "1.0.0",
  "description": "Obsidian 插件：在 Canvas 里绘制六边形虚构世界地图，并通过 Base 与世界观笔记互联（显示名 Project Kaki，译名 Project 垣）",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch",
    "build:esbuild": "node esbuild.config.mjs production",
    "test": "node --test --test-isolation=none",
    "test:list": "node --test --test-isolation=none --test-reporter=tap",
    "smoke": "node scripts/smoke.mjs",
    "deploy": "node scripts/deploy.mjs",
    "check": "node scripts/build.mjs && npm run test && node scripts/smoke.mjs && node scripts/deploy.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "esbuild": "^0.25.0",
    "obsidian": "latest",
    "typescript": "^5.9.0",
    "yaml": "^2.9.1"
  }
}
