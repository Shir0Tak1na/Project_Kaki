# 更新日志

本文件记录每个已发布版本的**用户可见变化**。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

> 说明：0.0.1 之前没有对外发布过，只在开发库里迭代。1.0.0 是第一个公开发布版。

## [未发布]

### 新增

- **自定义路径类型**：路径类型从写死的 4 种变成**可扩展的类型目录**（内置河流 / 道路 / 贸易路线 / 边界 + 你自己加的类型），
  每种类型各自保留画法参数：颜色 / 线宽 / 虚线 / 端点 / 连接。
  - 设置里新增「路径类型」一节：内置 4 种的**名字固定、参数可改**；最多 32 条自定义类型；
    「恢复出厂样式」只还原内置参数与字体，**不会删掉任何自定义定义**；
  - 画布工具条上原来的"每种类型一个按钮"改成**一个下拉**（内置在前、自定义在后），类型多了也不会把别的控件挤走；
  - **修掉一个会丢数据的缺陷：认不出的路径类型以前会让整条路径消失**。解析时把 `type` 置空、
    紧接着跳过整个对象 —— 打开一次、保存一次，那条路就**永久**没了。现在与未知地形 / 未知图标同一条口径：
    **原样保留 + 告警「已保留」**，回退视觉交给绘制层。路径照常画得出来，文件里的 `type` 一个字符都不改。
- **自定义区域类型**：6 个内置区域预设（王国 / 帝国 / 公国 / 教区 / 荒原 / 海域）变成**可扩展的类型目录**，
  每种类型各自保留 填充色 / 不透明度 / 边框色 / 边框宽 / 边框虚线
  （边框宽 0 = 不画边框；边框色留空 = 跟随填充色，也就是升级前的行为）。
  - 设置里新增「区域类型」一节；工具条上区域也改成**一个下拉**，每个选项带**自己的色块**（最多 32 条自定义类型）；
  - **旧地图不会被补字段**：升级前画的区域没有 `type`，读取时保持"没有"、写回时也**不补** ——
    老地图文件逐字节不变，图例仍然按颜色显示原来的类型名字；
  - 未知区域类型同样**原样保留 + 告警**：**"类型不认识"绝不导致区域被丢弃**。
- **定义文件：把四类自定义定义导出 / 导入**（地形 / 标记图标 / 路径类型 / 区域类型）。
  入口是「地图面板」→「文件与导出」里的两个动作，或命令面板里的
  **导出定义文件…（自定义地形/标记/路径类型/区域类型）** / **导入定义文件…**（同一份实现）。
  - 导出写在**库根目录**（`project-kaki-definitions-<日期>-<时分>.json`），**重名绝不覆盖**（自动加 `-2`、`-3`）；
    **内置类型不进文件**（它们每个库里本来就有，写进去只会得到一串"已有同 ID"）——
    代价是"内置类型的参数不随文件分享"，这是取舍，不是遗漏；
  - 导入**只做补充，绝不删东西**：同 ID 的条目保留**你现有的**定义；文件里**没有的那一段**
    （例如一份老文件里没有"区域类型"）**不会改动你现有的对应定义**；
  - 导入前先给你看一份**计划**：新增几条（按四类分列）、哪些因同 ID 被跳过及原因、哪一段文件里没有 ——
    对话框里说的与真的会做的来自**同一份计划**，不可能不一致；没有可新增条目时确认按钮是灰的；
  - 刚导出的文件立刻再导入 = **0 新增**（幂等，设置逐字段不变）；版本**接受 1 与 2**，
    比插件支持的更高时**明确拒绝**并提示升级插件；设置里一条自定义定义都没有时**不写文件**。
- **修掉工具条类型下拉的一个真实缺陷**：下拉展开时点画布想收起它，那一击**不再顺手在画布上落一个顶点**
  （以前会多出一笔你没打算画、还得撤销的东西）。收起之后的一击仍然照常交给画布。
- **修掉"调色模式下看不到图片入口"**：自定义地形的字形与图片两栏以前**只在「图片」模式下**渲染，
  于是「调色」模式里根本看不到「从库中选择…」，用户的感受就是"这个功能没有"。
  现在两栏**始终显示**，在调色模式下点它会**自动切到图片模式**再打开选择器；切回调色时图片路径仍然保留。

  然后在画布工具条与「放置标记」对话框里选用，笔记的 `map-type` 也能写它们。
  - 每条有**两种模式**（与自定义地形同一套做法）：**字形**（借用某个内置图标的形状，
    不依赖任何外部资源，新建时默认）或**图片**（用库内的一张图片）。
    **切换模式不会丢另一边的配置**（图片路径与字形都保留，切回来还在）；
  - **图片可以从库里选**：点「从库中选择…」从库内图片里挑，也可以手打库内相对路径
    （`png / jpg / jpeg / webp / svg / gif`）。选择器只列库内图片，库里没有图片时会明确提示；
    在「字形」模式下点这个按钮会**自动切到「图片」模式**（否则你会以为"选了没反应"）；
  - 设置页会**直接把这张图画出来**做预览（按原比例缩放，**不拉伸**）；取不到资源地址时，
    那里直接写明原因而不是显示一个破图；
  - **图片丢失或打不开时回退成这条标记的「字形」**（没配字形就是「通用」圆点），
    并在控制台给出一条可读原因 —— 标记**不会**因为图挂了就消失；
    同一张坏图只尝试一次、只告警一次（否则会变成每帧一次 404 + 每帧一条日志）；
  - 自定义标记排在工具条**内置 9 种之后**（不占用数字键 `1`–`9`），按钮上带显示名，
    悬停提示里有完整 ID；**图片模式下按钮上直接显示你那张图**，而不是借来的字形；
    放置标记对话框的图标下拉里同样列出它们（下拉里写显示名，写进文件的是 ID）；
  - **删掉设置里的定义不会删掉地图上的标记** —— 数据原样保留（文件里的 ID 不变），
    只是视觉退化成回退图钉，加回一条同 ID 的定义即可恢复；最多 60 条。
  - 图片目前**只画在画布上**：SVG / PNG 导出与 Base 缩略图里的标记一律是一个固定的小圆点
    （导出文件要能脱离你的库打开，内联图片属于后续工作）。
- **修掉一个会改坏数据的缺陷：未知的标记图标不再被改写成 `town`**。
  `markers[].icon` 原本按内置 9 种做白名单校验，认不出的名字（别的库、别的版本、
  或手工填进去的图标名）一律被替换成 `town` —— 打开、显示、导出都正常，
  **但你一保存，文件里的图标就被永久改写了**，而且没有任何提示。
  现在与地形 `t` 字段同一条口径：**未知值原样保留**，只在状态报告里计入「告警 N 条」
  并在控制台写清是哪个图标被保留了；回退视觉交给绘制层（画成回退图钉，标记不会消失）。
  只有"根本不是合法字符串"的值（非字符串、空串、含空白或控制符、超过 64 字符）才回退成 `town`，
  且原因与"未知图标"分开写。
- **导出地图时可以自己选范围**：新命令「导出地图…（可选范围与格式）」用一个对话框同时选
  **范围**（**全部内容** / **当前视口** / **某个区域**）与**格式**（SVG / PNG）；
  原来的「导出当前地图为 SVG / PNG」保留为快捷方式（= 全部内容 + 对应格式）。
  - 起因：导出与缩略图是"把全部内容等比缩进画布"，**一个远离主体的孤立格会把整张图压小**
    （实现时量过的一个例子：同一块 200×200 的区域，按区域导出占高约 709/1000 px，
    按全部内容只剩约 10/1000 px；具体比例取决于离群内容有多远）；
  - **按范围导出不丢内容**：范围外的内容仍然绘制，只是落在视野之外被裁掉，地图数据不变；
  - 文件名带上范围（全部内容不加后缀、`-视口`、`-区域名`），重名照旧加 `-2`、`-3`；
  - **失败时对话框留在原地**并给出可读原因，只提示一次，也不产出空文件。
- **图层开关移进侧栏「地图面板」**：六个开关（地形 / 网格 / 区域 / 路径 / 标记 / 名称）就在面板顶部，
  边看画布边切；设置页里也有同一组（改的是同一份设置）。
- **自定义地形分成「调色」与「图片」两种模式**：调色只用颜色 + 字形（不依赖任何外部资源，新建时默认）；
  图片用一个库内图片当图块，颜色降级为"加载失败时的回退色"。
  **切换模式不会丢另一边的配置**（图片路径与字形都保留，切回来还在）。
  旧设置会自动推断成对应模式，升级后画面与升级前一致。
- **自定义地形的图片可以从库里选**：设置页那一栏现在是"手打路径"或"「从库中选择…」"两条路都通。
  选择器只列库内图片（`png / jpg / jpeg / webp / svg / gif`），库里没有图片时会明确提示
  （Obsidian 没给插件系统文件对话框，所以只能从库里选；好处是地图文件里存的是库内相对路径，换机器也找得到）。
- **图片地形新增「显示方式」**：**单格一张**（每个六边形各一张，默认）或
  **整片一张（连通区域）**（所有连通的同类型格共用一张图：按"完整装下"缩放**保持比例**，
  且**只画在连通区域内** —— 超出部分裁掉，不会盖到旁边别的地形上）。
  "连通"按**六边形邻接**判定（中间隔着别的类型算两块）；旧设置自动推断为"单格一张"，升级后画面一致。
- **地图状态与诊断报告改为可复制 / 可导出的报告面板**：原来那条状态提示是一大块多行文本、
  在右上角停留 15 秒（正好压住侧边栏按钮区），而且**文字选不中、复制不了**。
  现在它是一个报告面板：正文可选中复制，还能一键**导出为库内文件**（重名自动加 `-2`）。

### 变更

- **路径与区域的样式设置从"全局颜色表"改成"按类型定义"**：设置页里原来的「4 种路径的默认颜色」与
  「6 个区域预设色」两栏，换成了「**路径类型**」与「**区域类型**」两节 ——
  内置类型与自定义类型各自的画法参数都在这里改。**已经画好的对象不受影响**：
  每条路径/区域把自己的颜色、线宽等存在地图文件里，改设置不会悄悄改掉你的地图。
- **移除画布工具条上的「地图层」按钮**。它是"停用整个地图层"，点了会连工具条自己一起收起来，
  关掉之后就再也点不到它了 —— 用户的反馈是"不知道是干什么的"。停用地图层的入口现在是
  侧栏面板里的动作与命令面板（面板常驻，关掉之后仍然可用）。
- **所有提示的显示时长收敛到 6 秒以内**（原来最长 15 秒）。需要"回头再查"的内容不再用提示承载，
  而是进报告面板；冒烟里加了一条全局断言盯着这个上界。

## [1.0.0] — 2026-09-23

第一个公开发布版。以下能力都在真实库里逐项验证过（清单见 `docs/USER-MANUAL.md` 与
`docs/ENGINEERING-NOTES.md` 附录；仍未验证的部分也诚实列在 `docs/ENGINEERING-NOTES.md` §7）。

### 新增

- **六边形地形绘制**：9 种内置地形（平原 / 森林 / 丘陵 / 山脉 / 水域 / 沼泽 / 沙漠 / 冻原 / 火山），
  笔刷半径可调，按住拖动即落笔；数字键 `1`–`9` 切换地形，`[` `]` 调笔刷。
- **路径**：河流、道路、贸易路线、边界。河流末端逐渐变细，名称**逐字贴着线条**排布。
- **区域**：半透明领域范围，名称按面积质心摆放（凹形状也落在形状内部）。
- **标记与文字标注**：地标可绑定笔记链接，点击直接跳转；文字标注随缩放保持可读字号。
- **三种几何模式**：**穿内部**（穿过格心）/ **沿格边**（勾出六边形边框）/ **逐边**（一次点一条边），
  用来画出整齐的国境线与领地轮廓。
- **撤销 / 重做**：一次拖动 = 一步历史；`Ctrl/Cmd+Z` / `Ctrl/Cmd+Shift+Z` 在绘制模式下生效。
- **地图面板**：右侧边栏视图，把常用命令变成按钮（不用每次翻命令面板）；侧边栏图标一键打开。
- **开发者模式**（默认关闭）：两个诊断探针命令平时从命令面板里隐藏，避免误触。
- **Base 自定义视图**（需 Obsidian 1.10.0+）：表格 + 地图缩略图 + 笔记坐标桥 ——
  笔记只要写了 `coordinates`，就会出现在地图与缩略图上，点击可跳转；坐标写错的行会被标红。
- **导出**：命令 **导出当前地图为 SVG**（导出与 Base 缩略图共用同一套世界坐标与配色）。
- **样式设置**：4 种路径的默认颜色、6 个区域预设色、名称字体族（可跟随主题），
  以及「恢复出厂样式」。颜色只影响**新画**的对象，不会改动已有地图。
- **自定义地形**：可以在设置里定义自己的地形类型（稳定的 ID + 显示名 + 颜色 + 可选的字形/图片），
  它和内置 9 种一起出现在工具条里，并一致地画到画布、Base 缩略图与 SVG 导出上。
  - ID 与显示名**解耦**：改显示名不会影响已经画好的格子（数据里存的是 ID）；
  - 三级回退：内置 → 你的定义 → 未知 ID（仍会画出一个可见的占位视觉，而不是让格子消失）；
  - 图片缺失/路径不合法时回退到"颜色 + 字形"，并给出可读原因；
  - **删除设置里的定义不会删掉地图上的格子** —— 数据原样保留，只是视觉退化；
  - **认不出的地形 ID 不再被丢弃**（以前保存一次就会永久删掉那些格子）—— 现在原样保留并告警；
  - 自定义地形**不占用数字键** `1`–`9`，最多 40 条（图集位图宽度所限）；
  - 图片目前**只画在画布上**：Base 缩略图与 SVG 导出用回退色（导出文件要能脱离你的库打开）。
- **图层开关**：地形 / 网格 / 区域 / 路径 / 标记 / 名称 六层可分别显示或隐藏。
  图层状态属于插件设置，**不会**写进地图数据。
- **图例**：从地图上**实际有的内容**生成（不是写死的清单），色块会体现路径的虚线样式；
  可随时隐藏。
- **PNG 导出**：命令 **导出当前地图为 PNG**，复用 SVG 导出的几何与配色
  （所以导出的位图和画布上看到的是一致的）。
- **标签页标题 / 侧栏**：地图面板显示为「地图」，工具条提供名称显示开关与地图层停用按钮。

### 技术说明（给使用者的一句话版本）

- 地图数据存在你库里的 `.map.md` 文件中（frontmatter + 一段 JSON 代码块），**纯文本、可 Git 管理**；
- 未知字段会原样保留，文件版本高于插件支持版本时**只读打开、绝不写回**；
- 写入前有安全闸：文件里没有地图标记就拒绝覆盖，普通笔记不会被误写；
- 插件用到 Obsidian Canvas 的未公开接口，因此对每个字段都做了形状守卫，
  探测失败会优雅停用并提示，而不是抛异常（排查用命令：**诊断当前 Canvas**）。

### 已知限制

- 桌面向：手势假设鼠标（双击、右键、中键），移动端与触控笔尚未适配；
- Base 表格一次性建表，未做虚拟滚动（几百行会变慢）；
- 撤销栈上限 100 步，不支持跨文件撤销；
- 图例目前只画在画布上，**导出文件里暂不带图例**（计划在后续版本补）；
- 样式设置只影响新画的对象，没有"把设置应用到已有对象"的一键重着色（那需要一个可撤销的新操作）。
