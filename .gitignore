# ============================================================
# Project Kaki · .gitignore
#
# 原则：能从源码重建的不入库；带本机绝对路径的不入库；
#      运行时会写出来的用户数据不入库。其余一律入库。
#
# ⚠️ 本文件**不支持行尾注释**：`main.js  # 打包产物` 会被当成一整条模式
#    （含空格与井号）从而永不匹配。注释必须单独占一行。
# ============================================================

# ---- 依赖 ----
node_modules/

# ---- 构建产物（都能由 node scripts/build.mjs 重建）----
.build/
main.js
*.js.map
*.tsbuildinfo

# 说明：如果你希望「clone 下来就能直接丢进 .obsidian/plugins/ 用」，
# 就把上面的 main.js 行删掉、改为提交它（代价是每次构建产生 450 KiB 的 diff，
# 好处是「仓库里的产物与源码是否一致」在 git status 里一眼可见）。

# ---- 本机环境相关（内容是本机绝对路径，换个机器就是错的）----
.npmrc
deploy.local.json

# ---- 编辑器 / IDE ----
.vscode/
.idea/

# ---- Obsidian 与系统 ----
data.json
.obsidian/
.DS_Store
Thumbs.db
desktop.ini

# ---- 临时文件 ----
*.log
*.tmp
*.bak
~$*
